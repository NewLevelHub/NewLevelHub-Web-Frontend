services:
  frontend:
    image: ${FRONTEND_IMAGE:-docker.io/aemadmin/newlevelhub-web-frontend:latest}
    pull_policy: always
    container_name: nlh_web_prod
    restart: unless-stopped
    ports:
      - "3001:3000"
    env_file:
      - .env.prod
