services:
  frontend:
    image: ${FRONTEND_IMAGE}
    pull_policy: always
    container_name: ${CONTAINER_NAME:-nlh_fe_preview}
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-20000}:80"
